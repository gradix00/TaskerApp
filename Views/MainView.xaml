<Window x:Class="MvvmTasker.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MvvmTasker.Views"
        mc:Ignorable="d"
        Title="{Binding Path=Title,Mode=OneWay}"
        MinHeight="350" MinWidth="500" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Style="{DynamicResource RoundedButton}" x:Name="OpenTasks" 
                Content="Taski"
                Background="CornflowerBlue"
                Foreground="White"
                Margin="2"/>

        <Button Grid.Column="1"
                Style="{DynamicResource RoundedButton}"
                x:Name="OpenCreatorNewTask" 
                Content="Nowy task" 
                Background="CornflowerBlue"
                Foreground="White"
                Margin="2"/>

        <ContentControl x:Name="ActiveItem" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
