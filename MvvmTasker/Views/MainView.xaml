<Window x:Class="MvvmTasker.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MvvmTasker.Views"
        mc:Ignorable="d"
        Title="{Binding Path=Title,Mode=OneWay}"
        MinHeight="350" MinWidth="500" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button x:Name="OpenSettings" Grid.Column="0" Content="Ustawienia" HorizontalAlignment="Stretch"
                Height="45"/>
        <Button x:Name="OpenCreatorNewTask" Grid.Column="1" Content="Nowy task" HorizontalAlignment="Stretch"
                Height="45"/>

        <ContentControl x:Name="ActiveItem" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
